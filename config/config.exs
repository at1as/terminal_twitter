# This file is responsible for configuring your application
# and its dependencies with the aid of the Mix.Config module.
use Mix.Config

# config/secrets.exs should contain all Twitter auth details
import_config "secrets.exs"
